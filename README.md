# useSimpleFetch hook

Simple and lightweight React hook for data fetching.

## Usage
```
npm i use-simple-fetch
```

```
const { data, isFetching, isError, error, refetch } = useFetch(url);
```

https://www.npmjs.com/package/use-simple-fetch